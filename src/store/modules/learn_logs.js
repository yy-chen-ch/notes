const baseUrl = 'http://localhost:10010/#'
const state = {
    logs: [
        { name: '模型小型化', link: baseUrl + '/model_smaller' }
    ]
}

const mutations = {

}
const actions = {
  
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
